resources:
    pipelines:
        combine_all_circuits_tables_pipeline:
            name: combine_all_circuits_tables_pipeline
            catalog: iedr-delta-catalog
            schema: silver
            libraries:
                - file:
                    path: ../src/iedr-transformation/silver/normalized_union/**
            serverless: true
            configuration:
                bundle.sourcePath: ${workspace.file_path}
                input.utilities.table: "circuit_normalized_table"
                target.table: "all_utilities_circuit_table"
#                input.utilities:
#                    - "utility_1"
#                    - "utility_2"