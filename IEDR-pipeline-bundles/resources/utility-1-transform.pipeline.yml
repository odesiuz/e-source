resources:
    pipelines:
        utility_1_transformation_pipeline:
            name: utility_1_transformation_pipeline
            catalog: ${variables.catalog.default}
            schema: silver
            libraries:
                - file:
                    path: ../src/iedr-transformation/silver/*.py
            serverless: true
            configuration:
                bundle.sourcePath: ${workspace.file_path}
                utility.id: "utility_1"