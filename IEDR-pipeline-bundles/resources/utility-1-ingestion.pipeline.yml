resources:
    pipelines:
        utility_1_ingestion_pipeline:
            name: utility_1_ingestion_pipeline
            catalog: iedr-delta-catalog
            schema: bronze
            libraries:
                - file:
                    path: ../src/iedr-ingestion/bronze/**
            serverless: true
            configuration:
                bundle.sourcePath: ${workspace.file_path}
                utility.id: "utility_1"