resources:
    pipelines:
        utility_2_ingestion_pipeline:
            name: utility_2_ingestion_pipeline
            catalog: iedr-delta-catalog
            schema: bronze
            libraries:
                - file:
                    path: ../src/bronze/**/*.py
            serverless: true
            configuration:
                bundle.sourcePath: ${workspace.file_path}
                utility.id: "utility_2"