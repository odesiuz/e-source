resources:
    pipelines:
        all_utility_union_pipeline:
            name: all_utility_union_pipeline
            catalog: iedr-delta-catalog
            schema: silver
            libraries:
                - file:
                    path: ../src/silver/normalized_union/**
            serverless: true
            configuration:
                bundle.sourcePath: ${workspace.file_path}